# ESP32/Arduino firmware (deferred)

This directory will contain the Arduino C++ firmware for the session controller when hardware is available.

**Required hardware**: ESP32 DevKit V1 (recommended) or Arduino Uno + USB serial to Raspberry Pi.

See the main spec document for the serial JSON protocol specification.
